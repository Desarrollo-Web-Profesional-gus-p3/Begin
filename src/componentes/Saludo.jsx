// const Saludo = (props) => {
//   return <h1>Holaaa, {props.nombre} y tu edad de chava es {props.edad}</h1>;
// };

// export default Saludo;

const Saludo = ({nombre, edad}) => {
  return <h1>Holaaa, {nombre} y tu edad de chava es {edad}</h1>;
};

export default Saludo;
